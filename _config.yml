# GitHub Pages Configuration
# This minimal config enables GitHub Pages without Jekyll processing

# Disable Jekyll processing since we're using pure HTML/CSS/JS
markdown: kramdown
highlighter: none

# Exclude files from the published site
exclude:
  - README.md
  - CLAUDE.md
  - .gitignore
  - .git
